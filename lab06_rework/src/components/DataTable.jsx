export default function DataTable() {
    return (
        <div className="w-full h-64 bg-white border-2 border-dashed border-gray-400 rounded-lg flex items-center justify-center text-gray-500">
            {/* Đây là khung giả để demo layout */}
            Placeholder cho bảng dữ liệu (DataTable)
        </div>
    );
}
